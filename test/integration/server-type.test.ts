// This test file is no longer needed since we only support StdioServer now
// The file is being kept as a placeholder in case we need to add integration tests for the StdioServer in the future

import { describe, test } from "bun:test";

describe("STDIO Server Integration Tests", () => {
  test("placeholder for future tests", () => {
    // Tests will be added when needed
  });
});
